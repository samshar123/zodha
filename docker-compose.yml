version: "3.8"

services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    # Optionally, if you are using volumes for development, uncomment:
    # volumes:
    #   - .:/app
